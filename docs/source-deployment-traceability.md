# 源码与生产部署对应关系

核验日期：2026-09-06。公开源码、测试通过和生产激活分别记录；源码可查看不等于线上镜像已经更新。

## 生产基线

| 对象 | 核验结果 |
| --- | --- |
| Gateway、Worker、Admin | 镜像标签 `voice-85dd734` |
| 对应源码 | `85dd734d5e9335337111d6db59cb8787be1415dc` |
| Health 公网 | `https://api.health.tellyouwhat.cn/readyz` 返回 HTTPS 200、`status=ready` |
| Journal 公网 | `https://api.journal.tellyouwhat.cn/readyz` 返回 HTTPS 200、`status=ready` |
| 主分支基线 | `caf942fe5aee2ec83050fea7d603c0d3428e8627`，不包含上述语音版本的全部源码 |

就绪接口只证明当前服务可接收请求，不证明真实购买、恢复权益、App Attest 或语音业务旅程已经验收。

## 候选源码

`codex/production-source-parity` 以已提交的 `a4297d3ef4e2b316bc385f6c7cc8b54457885fcd` 为基线。
它与生产源码 `85dd734` 的差异仅为两份手记说明和显式启用的真实供应商测试；普通测试不会运行该付费调用。
候选保留线上语音路由、订阅周期起点、App Attest 验签及数据库迁移，并包含以下限定修复：

| 改动 | 候选提交 | 对应审查 | 当前生产状态 |
| --- | --- | --- | --- |
| 运行镜像包含时区数据 | `4997e71` | [PR #1](https://github.com/FanciaZhang/tellyouwhat-backend/pull/1) | 现有镜像使用主机时区数据只读挂载；新镜像未部署 |
| 定时任务凭据交付、配置路径规范化和部署文件所有者约定 | `ecec6d1`、`f9b93bc`、`7afe328` | [PR #2](https://github.com/FanciaZhang/tellyouwhat-backend/pull/2) | 限定脚本及定时服务已应用，远程健康检查通过 |
| 新数据库备份排除临时 AI 任务数据，保留表结构 | `b0c81a6` | [PR #3](https://github.com/FanciaZhang/tellyouwhat-backend/pull/3) | 限定备份脚本已应用，新备份及隔离恢复通过；历史快照未改写 |
| AI 任务到期后禁止读取、取消及幂等重放旧结果 | `1cd9d3b` | [PR #4](https://github.com/FanciaZhang/tellyouwhat-backend/pull/4) | 已验证源码，尚未部署 |
| 后台任务按原始 token 预留窗口幂等对账，保护新一天/月用量 | 本候选 | [PR #5](https://github.com/FanciaZhang/tellyouwhat-backend/pull/5) | 尚未部署；旧预留标记保持保守扣量直至到期 |
| Health 方舟普通及流式请求关闭响应保存与上下文缓存 | 本候选 | [PR #5](https://github.com/FanciaZhang/tellyouwhat-backend/pull/5) | 尚未部署；meal decision 接入点的独立合成请求已验证参数兼容 |
| Health 响应必须成功完成，后台重试保留每次调用的未知用量额度 | 本候选 | [PR #5](https://github.com/FanciaZhang/tellyouwhat-backend/pull/5) | 尚未部署；保守预留不代表真实供应商账单或输出硬上限 |
| 后台每次调用先验证或预留额度，按尝试独立结算 | 本候选 | [PR #5](https://github.com/FanciaZhang/tellyouwhat-backend/pull/5) | 尚未部署；额度不可用时不发起模型调用，已发起调用的用量不依赖任务终态 |
| Health 供应商输出受显式上限约束，预算随签名授权及加密任务保存 | 本候选 | [PR #5](https://github.com/FanciaZhang/tellyouwhat-backend/pull/5) | 尚未部署；初始上限为 65,536 tokens，完整结果质量及更小的场景上限仍需评测 |

候选不包含其他工作区尚未提交的临时 IP 工具移除和文档改动。仓库中存在历史 IP 工具不代表生产仍启用该入口。
源码历史的有限凭据模式检查未发现私钥或常见访问令牌；该检查不构成完整安全审计或隐私保证。

## 部署前条件

1. 对统一候选的准确提交完成真实 MySQL/Redis 测试、合同生成检查、Swift 合同测试、运维及隔离恢复测试，以及六个服务镜像构建。旧主分支上的独立 PR 验证不能代替本候选验证。
2. 核对 GitHub `production` 环境的配置与当前服务器配置，尤其是手记语音开关、供应商凭据、模型、订阅商品、代理模式和镜像来源。只记录字段是否匹配，不把凭据值写入源码、日志或构建产物。
3. 记录候选完整提交与镜像摘要。按发布授权激活这一候选；保留当前健康版本及配置快照作为回滚来源。数据库迁移必须兼容已有语音版本。
4. 验证内部就绪、公网 TLS、真实 App 的鉴权/权益，以及健康和手记的业务回归。更新生产对应关系后，才能将候选标为已部署。

发布工作流会用 GitHub 保存的配置替换服务器配置；完成一致性核对前，不能直接以旧主分支运行部署。
备份保留、云服务商快照、撤回或删除后的恢复处理仍需独立核对，不能用任务接口到期校验替代物理删除证明。

## 后台尝试预算

每次 Worker 领取任务后，先验证预留归属、设备、金额及有效期。首次调用使用网关预付额度及原始计量窗口；第二、第三次尝试在调用前分别占用当时的日/月 token 额度。重复的同一尝试预留只扣一次，已结算的尝试不能重新使用折减后的额度。

模型调用返回后，独立结算该次尝试：有可靠用量时使用实际值，缺少用量时保留估算；取消、结果写入失败或其他尝试不会清除这一记录。调用前再次检查任务状态、到期和领取租约，供应商请求的 context 不超过任务有效期。Redis 不可用或应答不确定时不开始模型调用；应答不确定的预留保守保留。持续故障仍受原有三次 Worker 领取上限约束，可能使该任务失败，需要由客户端恢复流程重新提交。

旧的纯标记预留缺少归属和计量窗口，不能通过新 Worker 的调用前检查。部署应先停止旧 Worker 领取并处理在途任务，再切换网关与 Worker；不应让旧 Worker 在新候选启用后继续发起未预留的重试。未执行任务的保守预留可保留至原窗口到期，不能伪造旧预留归属或退款。

该控制不修改每日三次成功识别会话规则，也不是整个项目的现金上限。结算服务持续失败时仍保留估算，超估算实际用量需要可恢复台账进一步补记。每次尝试的完整长期用量台账、供应商账单对齐和实际执行并发控制仍需分别验证。

## 输出预算与兼容条件

Health 八个操作的普通、流式和后台方舟调用均发送 `max_output_tokens`，初始服务端预算 `health-output-v1` 为 65,536 tokens。Gateway 预留该上限及估算输入、Schema 和媒体额度；后台预算写入签名授权，并作为服务端字段保存在原有加密请求中。客户端公开请求不能设置或覆盖这一字段。排队、重新提交同一任务和 Worker 重试保留原授权预算，未知版本或缺少原预算的旧任务不获新调用授权。

当前模型的两次小型合成参数检查分别关闭思考和启用 medium 思考，均返回 HTTP 200、completed、65,536 的上限及合法 JSON，共使用输入 392、输出 65 tokens。该检查只证明当前接入点参数兼容；复杂餐食、长分析及思考与回答共享上限时的完整性仍需业务评测。App 自行配置供应商的直连路径尚未加入同一输出控制。其他执行策略中的模型、推理和超时尚未在本改动中形成完整快照。

`production.env.example` 对应的免费资源额度下限更新为每日 5,117,952 tokens，默认每月 158,656,512。这是为完整输出预留的配置容量，不是实际供应商账单或项目预算。已核验生产值仍为每日 4,749,312、每月 147,228,672，新配置校验会明确拒绝不足的每日容量。部署前须结合免费批次与重试规则、项目总预算核定额度，并同步 GitHub 和服务器配置；不得只替换镜像而沿用不兼容配置。

旧授权和任务的预算不会按新默认值补写。切换前需处理旧 Worker 的在途任务；缺少预算的历史结果仍可读取，但旧待执行任务会以 `execution_policy` 类别明确失败，客户端对该类别的恢复和重新提交仍需验收。本候选未执行生产配置修改、旧任务清理或二进制部署。
